#        /\             _
#       /  \        ___| |_ ___  _ __ _ __ ___  _   _
#      /    \      / __| __/ _ \| '__| '_ ` _ \| | | |
#     /      \     \__ \ || (_) | |  | | | | | | |_| |
#    /   ,,   \    |___/\__\___/|_|  |_| |_| |_|\___ |
#   /   |  |   \                                |___/
#  /_-''    ''-_\  stormyark.de

# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# Sourcing external config files

# User Configs

$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$UserConfigs = $HOME/.config/hypr/UserConfigs

#source= $UserConfigs/Startup_Apps.conf
#source= $UserConfigs/ENVariables.conf
#source= $UserConfigs/Monitors.conf
#source= $UserConfigs/Laptops.conf
#source= $UserConfigs/LaptopDisplay.conf
#source= $UserConfigs/WindowRules.conf
#source= $UserConfigs/UserKeybinds.conf
#source= $UserConfigs/UserSettings.conf
#source= $UserConfigs/WorkspaceRules.conf

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
monitor = ,1920x1080@60,1920x0,1
monitor = HDMI-A-1, preferred, auto, 1, mirror, eDP-1

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = thunar
$image = ristretto
$wofi = wofi --show drun
$rofi = rofi -theme ~/.config/rofi/launchernovo/themes/pywal-theme.rasi
$menu = $rofi
$browser = zen-browser
$Search_Engine = "https://www.duckduckgo.com/search?q={}"
$gmail = $browser --new-instance -P app "https://mail.google.com/" --class appProfile
$editor = code
#$notepad = code --profile notepad --unity-launch ~/Templates
#$launcher = tofi-drun -c ~/.config/tofi/tofi.launcher.conf
#$launcher_alt = tofi-run -c ~/.config/tofi/tofi.launcher.conf
$discord = discord-canary
$taskManager = btop

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)

#export XDG_CURRENT_DESKTOP=KDE
#export XDG_SESSION_DESKTOP=KDE
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 
#timeshift

# Execute your favorite apps at launch like this:

# wallpaper stuff / More wallpaper options below
exec-once = swww query || swww-daemon
#exec-once = $SwwwRandom $wallDIR # random wallpaper switcher every 30 minutes

exec-once = nm-applet --indicator &
exec-once = blueman-applet &
exec-once = waybar &
exec-once = swaync &
exec-once = hypridle #lock/suspend when idle
# exec-once = hyprpicker 
exec-once = [workspace 1] $terminal
exec-once = [workspace 2 silent] $browser

exec-once = bash ~/.config/hypr/autostart.conf
exec-once = wl-clipboard-history
exec-once = wl-paste --watch cliphist store
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
#exec-once = rm "$HOME/.cache/cliphist/db"
#exec-once = notify-send -a aurora "hello $(whoami)" &

#exec-once = /usr/bin/synology-drive

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

#dark theme
#env = QT_QPA_PLATFORMTHEME,qt5ct
#env = QT_STYLE_OVERRIDE,Kvantum

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general

# Sourcing colors generated by wallust
source = $HOME/.config/hypr/wallust/wallust-hyprland.conf

general {
    gaps_in = 3 #5
    gaps_out = 8 #20

    border_size = 2 #2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    #col.active_border = $color12 #rgba(33ccffee) rgba(00ff99ee) 45deg
    #col.inactive_border = $color10 #rgba(595959aa)

    col.active_border = rgb(222222)
    col.inactive_border = rgb(171717)
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 12
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 0.93
    inactive_opacity = 0.8
    fullscreen_opacity = 1.0

    dim_inactive = false
    dim_strength = 0.1
    dim_special = 0.8

    shadow {
        enabled = false #true
        range = 6 #4
        render_power = 1 #3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 6 #3
        passes = 3 #2
        new_optimizations = true
        xray = true
        ignore_opacity = true
        vibrancy = 0.1696
        brightness = 1
        noise = 0.0200 #0.10
        contrast = 1
        special = true
        popups = true
        popups_ignorealpha = 0.6
    }
    # Blurring layerSurfaces
    # blurls = gtk-layer-shell
    # blurls = waybar
    # blurls = lockscreen
    # blurls = rofi
    blurls = wofi
    blurls = mako
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    #animation = border, 1, 5.39, easeOutQuint
    animation = border, 1, 10, default
    #animation = windows, 1, 4.79, easeOutQuint
    animation = windows, 1, 6, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    #animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = windowsOut, 1, 5, linear, popin 80%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    #animation = fade, 1, 3.03, quick
    animation = fade, 1, 7, default
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    #animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspaces, 1, 6, default
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

windowrulev2 = suppressevent maximize, class:.*

# windowrulev2 = opacity 0.7 0.7, class:^(kitty)$

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = de
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    repeat_rate=50
    repeat_delay=300
    numlock_by_default=1
    left_handed=0
    follow_mouse=1
    float_switch_override_focus=0

    numlock_by_default = true
    left_handed = false
    follow_mouse = true
    float_switch_override_focus = false

    sensitivity = 0.1 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        disable_while_typing = 1
        natural_scroll = 1
        clickfinger_behavior = 0
        middle_button_emulation = 1
        tap-to-click = 1
        drag_lock = 0
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    gesture = 3, horizontal, workspace
    #workspace_swipe = true
    #workspace_swipe_fingers = 3
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, B, togglefloating, #V
#bind = $mainMod, R, exec, $menu
#bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Application launcher
bind = ALT, SPACE, exec, $menu -show drun
#bind = ALT, SPACE, exec, $menu #wofi

# Clipboard binds
#bind = $mainMod SHIFT, C, exec, wl-copy, notify-send "copied to clipboard" #copy
bind = $mainMod SHIFT, C, exec, bash -c 'wl-copy && notify-send "copied to clipboard"' #copy
bind = $mainMod SHIFT, V, exec, bash -c "$(wl-paste)" #paste
#bind = $mainMod, V, exec, wl-paste | wl-copy && wl-paste --primary | wl-copy
bind = $mainMod, V, exec, cliphist list | $menu -dmenu | cliphist decode | wl-copy #Clipboard history
bind = $mainMod SHIFT, X, exec, bash "cliphist list | fzf | cliphist decode | bash" #Cut
bind = $mainMod, period, exec, rofimoji --selector rofi --rofi-args "-theme ~/.config/rofi/launchernovo/themes/pywal-theme.rasi" #Emoji Selector

# Wallpaper
bind = $mainMod, W, exec, $UserScripts/WallpaperSelect.sh

# Task Manager
bind = Ctrl_Shift, Tab, exec, $terminal $taskManager

# Applications
bind = , Print, exec, hyprshot -m region
bind = $mainMod SHIFT, S, exec, hyprshot
bind = $mainMod, P, exec, hyprpicker -a
bind = $mainMod, L, exec, hyprlock #lock the screen
#bind = $mainMod, D, exec, ~/.config/scripts/toggle_desktop #show desktop
bind = $mainMod, D, workspace, empty
#bind = $mainMod, F, exec, ~/.config/wofi/scripts/launcher.sh
bind = $mainMod, T, exec, $browser #browser
#bind = $mainMod, O, exec, $gmail
bind = ALT, RETURN, fullscreen #fullscreen

# Cycle through workspaces with Alt + Tab
bind = ALT, Tab, exec, ~/.config/scripts/cycle-workspaces.sh

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move focus with mainMod + vim keys
#bind = $mainMod, H, movefocus, l
#bind = $mainMod, L, movefocus, r
#bind = $mainMod, K, movefocus, u
#bind = $mainMod, J, movefocus, d

# Resize windows
binde = $mainMod SHIFT, left, resizeactive,-50 0
binde = $mainMod SHIFT, right, resizeactive,50 0
binde = $mainMod SHIFT, up, resizeactive,0 -50
binde = $mainMod SHIFT, down, resizeactive,0 50

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
